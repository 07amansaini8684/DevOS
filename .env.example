# Gemini – must be VITE_* so the client can read it
VITE_GEMINI_API_KEY=your_gemini_api_key

# Tambo AI – get your key at https://tambo.co/dashboard
# Required for the workspace AI chat (generative UI).
VITE_TAMBO_API_KEY=
